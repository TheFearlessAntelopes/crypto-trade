import { OptionalDirective } from './optional.directive';

describe('OptionalDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new OptionalDirective();
  //   expect(directive).toBeTruthy();
  // });
});
