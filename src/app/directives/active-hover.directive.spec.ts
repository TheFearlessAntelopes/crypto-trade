import { ActiveHoverDirective } from './active-hover.directive';

describe('ActiveHoverDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new ActiveHoverDirective();
  //   expect(directive).toBeTruthy();
  // });
});
